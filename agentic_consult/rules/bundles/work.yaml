# Work Rules
# Standard rules for professional email management.

rules:
  # Too broad for personal email (flags everything) - discovered in testing.
  - id: work-important-direct
    action: review
    condition: >
      Email contains a message, request, or activity originating from or regarding
      a known Customer or VIP (including proxied notifications from Slack, Jira,
      or Docs), or is addressed directly to the user.

  - id: work-calendar-invite
    action: review
    condition: >
      Email is a calendar invitation, update, or cancellation for a meeting.

  - id: work-document-share
    action: review
    condition: >
      Email indicates a document, spreadsheet, or slide deck was shared with the user.

  - id: work-access-request
    action: review
    condition: >
      Email is a request for access to a file, folder, or resource owned by the user.

# DEFAULT STATE: DISABLED
# To enable these rules, add "work-*" to the 'enable' list in your 
# user configuration file (e.g. config://email.yaml).
disable:
  - "work-*"
